# Changelog

规则库文档遵循「一个 skill 一个文件树」的朴素版本策略：`main` 分支始终可用，版本号只标记
有实质规则变化的节点。

## 0.2.0 — 2026-08-29

**新增：色板驱动模式（palette-driven）**

- 用户只给颜色（hex / RGB / 色号 / 中文色名 / 一段配色描述）而没有主题时，不再反问主题：
  六步推演 —— 解析归一 → 按明度判角色 → 收敛到画风 → 补齐四色 → 反查主题 → 命名。
- 立场：用户给的颜色是**种子**，不是成品配色。只继承色相，面积、明度阶梯、饱和度按体系重排。
- 锁色为硬规则：「就用这个颜色」「别改」「这是品牌色」时，该 hex 与强制文字同级，逐字保留，
  改为调整其余三个衍生色、材质与明暗去成就它。
- 交付格式新增「配色推演表」：必须回显用户原始 hex，并写明每个色改了什么（例 `S 50→22, L 53→72`）；
  被锁定的色标「原色未改」。
- 快查卡新增：中文色名近似表（12 色，附 HSL）、种子色角色判定表、收敛参数、四色板组装模板、
  色相→主题反查表（10 档）、冲突与锁定处理表、完整数值示例、出图二次收敛短语。
- 边界情况新增 4 条：只有颜色不追问、颜色太艳先柔化并说明、互撞色给两版让用户选、
  成图被模型拉艳时二次收敛重跑。

**打包与许可**

- 拆出 `lite/` 自包含分发包（9 组代表色板 + 完整推演表），推荐作为分发/上架单元。
- 补 `LICENSE`：CC BY-NC 4.0（署名 · 非商业使用），并明确**产出物不受许可约束**。
- 补 `README.md` 与本 changelog。
- 原有规则零删改：诗意主题命名、情绪→配色映射、单一元素、六种构图骨架、四叠加项模式体系、
  字体版权安全、强制文字处理、A/B 双版标题输出全部保留。

## 0.1.0 — 2026-08-26

初始体系：

- 「静谧自然海报」审美基线：低饱和、大面积留白、胶片/纸张颗粒、柔焦边缘、一个光线焦点。
- 诗意主题命名规则（一个标题只留一个核心意象 + 一个动作）。
- 主题情绪 → 自动配色映射；26 组色板库，含 hex、适合主题、气质标签与面积比例。
- 单一元素拆解：1 主元素 + 0–2 辅助元素 + 1 光线焦点。
- 六种构图骨架：中轴悬浮 / 上实下虚 / 对角线意象 / 低位地平线 / 偏侧留白 / 小人物远景。
- 「一个基础 + 四个可叠加项」模式体系：基础「安静自然」，叠加「编辑排版 / 档案图鉴 / 电影夜色 /
  实验笔触」，每张最多两项。
- 标题设计速查（11 种排版骨架、字体气质三选一、字色策略三选一、融合手法库）。
- 默认输出「干净克制版 × 大胆飞扬版」两套标题方案，主色与主题必须一致。
- 字体版权安全规则、强制文字逐字保留规则、交付前检查清单。
- 提示词模板（标准版 + 无字底图版）与共用负向提示词。
